# Default values for synstream-console (Umbrella Chart).
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

# Global configuration shared across all components
global:
  # Image pull secrets for private registry access
  imagePullSecrets: []
    # - name: ghcr-imagepullsecret
  
  # Default image registry and repository
  image:
    registry: ghcr.io
    repository: rootsinnovation
    pullPolicy: Always

# =============================================================================
# Frontend Console (Deployment)
# =============================================================================
console:
  enabled: true
  replicaCount: 1
  
  image:
    name: synstream-console-project
    tag: "v1.2.0"
  
  service:
    type: ClusterIP
    port: 80
    annotations: {}
  
  serviceAccount:
    create: true
    automount: true
    annotations: {}
    name: ""
  
  resources:
    limits:
      cpu: 100m
      memory: 128Mi
    requests:
      cpu: 50m
      memory: 64Mi
  
  podAnnotations: {}
  podLabels: {}
  nodeSelector: {}
  tolerations: []
  affinity: {}

# =============================================================================
# Nginx Gateway (Deployment)
# =============================================================================
nginx:
  enabled: true
  replicaCount: 1
  
  image:
    name: nginx
    tag: "latest"
    # Override to use Docker Hub official nginx image
    registry: "docker.io"
    repository: "library"
  
  service:
    type: LoadBalancer
    port: 80
    annotations: {}
  
  resources:
    limits:
      cpu: 100m
      memory: 128Mi
    requests:
      cpu: 50m
      memory: 64Mi
  
  # Nginx configuration
  config:
    # CORS allow-list for the auth API (/api/v2). One nginx map regex — set this
    # instance's Console + Pipeline origins here. Rendered into the map in
    # templates/nginx-configmap.yaml so it is per-instance without editing the nginx
    # block below. Single-quoted so the regex backslashes survive YAML intact.
    corsAllowedOriginRegex: '~^https?://(console-page-nonprod\.rtsinv\.com|pipeline-studio-nonprod\.rtsinv\.com|localhost(:[0-9]+)?)$'
    defaultConf: |
      # Security response headers, shared by all locations.
      # Kept in a snippet file so both the server scope and the /api/v2 location
      # (which defines its own add_header and therefore does NOT inherit) stay in sync.
      # TISSA/HostedScan remediation: #10 anti-clickjacking, #11 CSP not set, #13 nosniff.

      server {
          listen 80;
          server_name _;

          # Applied at server scope -> inherited by "location /" (serves the scanned HTML/SPA).
          add_header X-Frame-Options "SAMEORIGIN" always;
          add_header X-Content-Type-Options "nosniff" always;
          add_header Referrer-Policy "strict-origin-when-cross-origin" always;
          add_header Permissions-Policy "geolocation=(), microphone=(), camera=(), payment=()" always;
          add_header Content-Security-Policy "default-src 'self'; script-src 'self'; style-src 'self'; img-src 'self' data: blob:; font-src 'self' data:; connect-src 'self'; frame-src 'self'; frame-ancestors 'self'; base-uri 'self'; form-action 'self'; object-src 'none'" always;

          location /api/v2 {
              if ($request_method = OPTIONS) {
                  add_header Access-Control-Allow-Origin $cors_origin always;
                  add_header Access-Control-Allow-Credentials true always;
                  add_header Access-Control-Allow-Methods "GET, POST, PUT, DELETE, PATCH, OPTIONS" always;
                  add_header Access-Control-Allow-Headers "Content-Type, Authorization, X-Requested-With, Origin, Accept" always;
                  add_header Access-Control-Max-Age 86400 always;
                  add_header Content-Length 0;
                  add_header Content-Type text/plain;
                  return 204;
              }

              # Existing CORS for the auth API.
              add_header Access-Control-Allow-Origin $cors_origin always;
              add_header Access-Control-Allow-Credentials true always;
              add_header Access-Control-Expose-Headers "Authorization, Set-Cookie" always;

              # Repeated here: nginx add_header does not inherit into a location that
              # defines its own add_header directives.
              add_header X-Frame-Options "SAMEORIGIN" always;
              add_header X-Content-Type-Options "nosniff" always;
              add_header Referrer-Policy "strict-origin-when-cross-origin" always;
              add_header Permissions-Policy "geolocation=(), microphone=(), camera=(), payment=()" always;
              add_header Content-Security-Policy "default-src 'self'; script-src 'self'; style-src 'self'; img-src 'self' data: blob:; font-src 'self' data:; connect-src 'self'; frame-src 'self'; frame-ancestors 'self'; base-uri 'self'; form-action 'self'; object-src 'none'" always;

              proxy_pass http://synstream-auth:8080;
              proxy_set_header Host $host;
              proxy_set_header X-Real-IP $remote_addr;
              proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
              proxy_set_header X-Forwarded-Proto $scheme;
          }

          location / {
              proxy_pass http://synstream-console-project:80;
              proxy_set_header Host $host;
              proxy_set_header X-Real-IP $remote_addr;
              proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
              proxy_set_header X-Forwarded-Proto $scheme;
          }
      }

  podAnnotations: {}
  nodeSelector: {}
  tolerations: []
  affinity: {}

# =============================================================================
# Auth Service (StatefulSet)
# =============================================================================
auth:
  enabled: true
  replicaCount: 1

  # Extra environment variables for the auth container.
  # Recommended: provide the license HMAC secret via a pre-created K8s Secret
  # (do NOT commit the secret value to the chart). The Secret must hold the same
  # value the operator and license-cli sign/verify with. If left empty, the auth
  # service falls back to the legacy hardcoded secret.
  #
  # extraEnv:
  #   - name: LICENSE_JWT_SECRET
  #     valueFrom:
  #       secretKeyRef:
  #         name: synstream-license-secret
  #         key: secret
  extraEnv: []

  image:
    name: synstream-auth-project
    tag: "v1.2.0"
  
  service:
    type: ClusterIP
    port: 8080
    annotations: {}
  
  serviceAccount:
    create: true
    automount: true
    annotations: {}
    name: ""
  
  # Persistent storage configuration
  storage:
    enabled: true
    size: 1Gi
    accessMode: ReadWriteOnce
    # storageClass: ""  # Use default storage class
  
  resources:
    limits:
      cpu: 500m
      memory: 512Mi
    requests:
      cpu: 100m
      memory: 128Mi
  
  securityContext:
    runAsNonRoot: true
    runAsUser: 1001
    allowPrivilegeEscalation: false
    readOnlyRootFilesystem: false
    capabilities:
      drop:
        - ALL
  
  podSecurityContext:
    fsGroup: 1001
  
  # Configuration for auth service
  config:
    serverHost: "0.0.0.0"
    serverPort: "8080"
    databaseType: "sqlite"
    databasePath: "/app/data/auth.db"
    jwtAccessTokenDuration: "86400"
    jwtRefreshTokenDuration: "604800"
    cookieEnabled: "true"
    cookieDomain: ""
    cookiePath: "/"
    cookieSecure: "false"
    cookieHttpOnly: "true"
    cookieSameSite: "lax"
    corsEnabled: "true"
    corsAllowCredentials: "true"
    corsMaxAge: "86400"
    kubernetesConfigType: "incluster"
    kubernetesKubeconfigPath: ""
    logOutput: "stdout"
    logAuditFile: "/app/data/audit.log"
    logFallbackFile: "/app/data/fallback.log"

  # Sensitive auth config, rendered into the synstream-auth-secret Secret.
  # Supply real values at install time (--set auth.secrets.jwtSecretKey=...,
  # auth.secrets.encryptionKey=...) or via an external/sealed secret. Leaving
  # these empty makes the auth service fail fast at startup rather than booting
  # with an insecure default.
  #   jwtSecretKey:  generate with: openssl rand -base64 48
  #   encryptionKey: exactly 32 bytes, e.g.: openssl rand -base64 32 | head -c 32
  secrets:
    jwtSecretKey: ""
    encryptionKey: ""

  podAnnotations: {}
  nodeSelector: {}
  tolerations: []
  affinity: {}

# =============================================================================
# AI Service (Deployment)
# =============================================================================
ai:
  enabled: true
  replicaCount: 1
  
  image:
    name: synstream-ai-project
    tag: "v1.2.0"
  
  service:
    type: ClusterIP
    port: 8000
    annotations: {}
  
  serviceAccount:
    create: true
    automount: true
    annotations: {}
    name: ""
  
  resources:
    limits:
      cpu: 1000m
      memory: 1Gi
    requests:
      cpu: 100m
      memory: 256Mi
  
  livenessProbe:
    httpGet:
      path: /health
      port: http
    initialDelaySeconds: 10
    periodSeconds: 10
    timeoutSeconds: 3
    failureThreshold: 3
  
  readinessProbe:
    httpGet:
      path: /health
      port: http
    initialDelaySeconds: 5
    periodSeconds: 5
    timeoutSeconds: 3
    failureThreshold: 3
  
  # Configuration for AI service
  config:
    port: "8000"
    authServerUrl: "http://synstream-auth:8080"
    authPath: "/api/v2/synstream/token"
    disableAuth: "false"
    allowedOrigins: "*"
    defaultModelId: "gemini-flash"
    # OpenAI Models
    openai0Id: "openai-gpt5"
    openai0Name: "OpenAI GPT-5"
    openai0Model: "gpt-5-mini"
    openai1Id: "openai-gpt35"
    openai1Name: "OpenAI GPT-3.5"
    openai1Model: "gpt-3.5-turbo"
    # Gemini Models
    gemini0Id: "gemini-flash"
    gemini0Name: "Gemini Flash"
    gemini0Model: "gemini-2.0-flash"
    # Anthropic Models
    anthropic0Id: "claude-opus"
    anthropic0Name: "Claude Opus"
    anthropic0Model: "claude-3-opus-20240229"
    # OpenAI-Compatible Models
    openaiCompatible0Id: "custom-llm"
    openaiCompatible0Name: "Custom LLM"
    openaiCompatible0Model: "openai/gpt-oss-20b"
    openaiCompatible0Url: "http://localhost:1234/v1"
    logOutput: "stdout"
    logAuditFile: "/app/data/audit.log"
    logFallbackFile: "/app/data/fallback.log"
  
  # AI API secrets (should be provided via values or external secret management)
  secrets:
    # Set to true to create a Secret resource
    create: false
    # API keys for AI services
    openai0ApiKey: ""
    openai1ApiKey: ""
    gemini0ApiKey: ""
    anthropic0ApiKey: ""
    openaiCompatible0ApiKey: ""
  
  podAnnotations: {}
  nodeSelector: {}
  tolerations: []
  affinity: {}

# =============================================================================
# Ingress Configuration
# =============================================================================
ingress:
  enabled: false
  className: "nginx"
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-nginx
    # kubernetes.io/ingress.class: nginx
    # kubernetes.io/tls-acme: "true"
  
  hosts:
    - host: console.rtsinv.com
      paths:
        - path: /
          pathType: Prefix
  
  tls:
    - secretName: letsencrypt-nginx
      hosts:
        - console.rtsinv.com
